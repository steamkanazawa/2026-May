    function Windows() {
      document.getElementById("content").innerHTML = `
        <section>
  <h2>WindowsでMinecraft Java版サーバーを立てる方法</h2>

  <p>
    ここでは、WindowsでMinecraft Java版の公式サーバーを立てる方法を紹介します。
    統合版・Bedrock版ではなく、Java版向けの手順です。
  </p>

  <h3>1. サーバー用フォルダを作る</h3>

  <p>
    まず、デスクトップなど分かりやすい場所に、サーバー用のフォルダを作ります。
  </p>

  <p>例：</p>

  <pre><code>MinecraftServer</code></pre>

  <p>
    次に、Minecraft公式サイトからサーバー用のjarファイルをダウンロードし、
    作成したフォルダの中に入れます。
  </p>

  <p>
    ファイル名はバージョンによって異なりますが、分かりやすくするために、
    ここでは次の名前に変更しておきます。
  </p>

  <pre><code>server.jar</code></pre>

  <h3>2. 起動用のbatファイルを作る</h3>

  <p>
    サーバー用フォルダの中で右クリックし、
    「新規作成」→「テキスト ドキュメント」を選びます。
  </p>

  <p>
    作成したテキストファイルを開き、次の内容を入力します。
  </p>

  <pre><code>@echo off
java -Xmx4G -Xms4G -jar server.jar nogui
pause</code></pre>

  <p>
    保存したら、ファイル名を次のように変更します。
  </p>

  <pre><code>start.bat</code></pre>

  <p>
    このとき、ファイル名が <code>start.bat.txt</code> になっていると動きません。
    Windowsのエクスプローラーで「ファイル名拡張子」を表示して、
    拡張子が <code>.bat</code> になっていることを確認してください。
  </p>

  <h3>3. 一度サーバーを起動する</h3>

  <p>
    <code>start.bat</code> をダブルクリックします。
  </p>

  <p>
    初回起動時は、すぐに画面が止まる、またはサーバーが終了する場合があります。
    これは正常です。サーバーフォルダの中に、設定ファイルや
    <code>eula.txt</code> などのファイルが自動で作成されます。
  </p>

  <h3>4. EULAに同意する</h3>

  <p>
    サーバーフォルダの中にある <code>eula.txt</code> をメモ帳などで開きます。
  </p>

  <p>
    中に次の行があります。
  </p>

  <pre><code>eula=false</code></pre>

  <p>
    これを次のように変更します。
  </p>

  <pre><code>eula=true</code></pre>

  <p>
    変更したら保存して閉じます。
  </p>

  <p>
    これはMinecraftの利用規約に同意するための設定です。
    内容を確認したうえで変更してください。
  </p>

  <h3>5. もう一度サーバーを起動する</h3>

  <p>
    もう一度 <code>start.bat</code> をダブルクリックします。
  </p>

  <p>
    黒いコマンド画面が開き、ログが流れ始めます。
    しばらく待って、最後のほうに次のような表示が出ればサーバーの起動は成功です。
  </p>

  <pre><code>Done</code></pre>

  <h3>6. 自分のPCから接続する</h3>

  <p>
    同じPCでMinecraftを起動し、「マルチプレイ」からサーバーを追加します。
  </p>

  <p>
    サーバーアドレスには、次のどちらかを入力します。
  </p>

  <pre><code>localhost</code></pre>

  <p>または</p>

  <pre><code>127.0.0.1</code></pre>

  <p>
    これで、自分のPC上で動いているサーバーに接続できます。
  </p>

  <h3>7. 同じ家の人に接続してもらう場合</h3>

  <p>
    同じWi-Fiや同じLAN内のPCから接続する場合は、
    サーバーを立てているPCのローカルIPアドレスを使います。
  </p>

  <p>
    ローカルIPアドレスは、コマンドプロンプトで次のコマンドを実行すると確認できます。
  </p>

  <pre><code>ipconfig</code></pre>

  <p>
    表示された中から「IPv4 アドレス」を探します。
  </p>

  <p>例：</p>

  <pre><code>192.168.1.10</code></pre>

  <p>
    他のPCでは、MinecraftのサーバーアドレスにこのIPアドレスを入力します。
  </p>

  <h3>8. 外部の友達に接続してもらう場合</h3>

  <p>
    家の外にいる友達に接続してもらうには、
    通常はルーター側でポート開放が必要です。
  </p>

  <p>
    Minecraft Java版サーバーの標準ポートは次の番号です。
  </p>

  <pre><code>25565</code></pre>

  <p>
    ルーターの管理画面で、サーバーを立てているPCに対して、
    TCPの25565番ポートを開放します。
  </p>

  <p>
    ただし、ポート開放はネットワーク設定やセキュリティに関わる作業です。
    よく分からない場合は、無理に設定しないほうが安全です。
    身内だけで遊ぶ場合は、VPN系のサービスやレンタルサーバーを使う方法もあります。
  </p>

  <h3>9. サーバーを終了する方法</h3>

  <p>
    サーバーを終了するときは、コマンド画面をそのまま閉じるのではなく、
    画面に次のコマンドを入力します。
  </p>

  <pre><code>stop</code></pre>

  <p>
    これでワールドデータが保存され、安全にサーバーを停止できます。
  </p>
</section>
      `;
    }

    function Mac() {
      document.getElementById("content").innerHTML = `<section>
  <h2>MacでMinecraft Java版サーバーを立てる方法</h2>

  <p>
    ここでは、MacでMinecraft Java版の公式サーバーを立てる方法を紹介します。
    統合版・Bedrock版ではなく、Java版向けの手順です。
  </p>

  <h3>1. サーバー用フォルダを作る</h3>

  <p>
    まず、デスクトップなど分かりやすい場所に、サーバー用のフォルダを作ります。
  </p>

  <p>例：</p>

  <pre><code>MinecraftServer</code></pre>

  <p>
    次に、Minecraft公式サイトからサーバー用のjarファイルをダウンロードし、
    作成したフォルダの中に入れます。
  </p>

  <p>
    ファイル名はバージョンによって異なりますが、分かりやすくするために、
    ここでは次の名前に変更しておきます。
  </p>

  <pre><code>server.jar</code></pre>

  <h3>2. Javaが入っているか確認する</h3>

  <p>
    Macでサーバーを動かすにはJavaが必要です。
    まず、ターミナルを開いて次のコマンドを入力します。
  </p>

  <pre><code>java -version</code></pre>

  <p>
    Javaのバージョン情報が表示されれば、Javaはインストールされています。
    エラーが出る場合は、Javaをインストールしてから次に進んでください。
  </p>

  <h3>3. 起動用のshファイルを作る</h3>

  <p>
    サーバー用フォルダの中に、起動用のファイルを作ります。
    テキストエディットなどを開き、次の内容を入力します。
  </p>

  <pre><code>#!/bin/bash
cd "$(dirname "$0")"
java -Xmx4G -Xms4G -jar server.jar nogui</code></pre>

  <p>
    入力できたら、次のファイル名でサーバー用フォルダの中に保存します。
  </p>

  <pre><code>start.sh</code></pre>

  <p>
    テキストエディットを使う場合は、標準テキストではなくプレーンテキストで保存してください。
    ファイル名が <code>start.sh.txt</code> にならないように注意します。
  </p>

  <h3>4. start.shに実行権限を付ける</h3>

  <p>
    Macでは、作成した <code>start.sh</code> に実行権限を付ける必要があります。
    ターミナルを開き、次のように入力します。
  </p>

  <pre><code>cd ~/Desktop/MinecraftServer
chmod +x start.sh</code></pre>

  <p>
    サーバー用フォルダをデスクトップ以外に作った場合は、
    <code>cd</code> の後ろのパスを自分の環境に合わせて変更してください。
  </p>

  <h3>5. 一度サーバーを起動する</h3>

  <p>
    ターミナルでサーバー用フォルダに移動した状態で、次のコマンドを実行します。
  </p>

  <pre><code>./start.sh</code></pre>

  <p>
    初回起動時は、すぐにサーバーが終了する場合があります。
    これは正常です。サーバーフォルダの中に、設定ファイルや
    <code>eula.txt</code> などのファイルが自動で作成されます。
  </p>

  <h3>6. EULAに同意する</h3>

  <p>
    サーバーフォルダの中にある <code>eula.txt</code> を開きます。
  </p>

  <p>
    中に次の行があります。
  </p>

  <pre><code>eula=false</code></pre>

  <p>
    これを次のように変更します。
  </p>

  <pre><code>eula=true</code></pre>

  <p>
    変更したら保存して閉じます。
  </p>

  <p>
    これはMinecraftの利用規約に同意するための設定です。
    内容を確認したうえで変更してください。
  </p>

  <h3>7. もう一度サーバーを起動する</h3>

  <p>
    もう一度ターミナルで次のコマンドを実行します。
  </p>

  <pre><code>./start.sh</code></pre>

  <p>
    ターミナルにログが流れ始めます。
    しばらく待って、最後のほうに次のような表示が出ればサーバーの起動は成功です。
  </p>

  <pre><code>Done</code></pre>

  <h3>8. 自分のMacから接続する</h3>

  <p>
    同じMacでMinecraftを起動し、「マルチプレイ」からサーバーを追加します。
  </p>

  <p>
    サーバーアドレスには、次のどちらかを入力します。
  </p>

  <pre><code>localhost</code></pre>

  <p>または</p>

  <pre><code>127.0.0.1</code></pre>

  <p>
    これで、自分のMac上で動いているサーバーに接続できます。
  </p>

  <h3>9. 同じ家の人に接続してもらう場合</h3>

  <p>
    同じWi-Fiや同じLAN内の端末から接続する場合は、
    サーバーを立てているMacのローカルIPアドレスを使います。
  </p>

  <p>
    ローカルIPアドレスは、ターミナルで次のコマンドを実行すると確認できます。
  </p>

  <pre><code>ipconfig getifaddr en0</code></pre>

  <p>
    Wi-Fiではなく有線LANを使っている場合は、次のコマンドで確認できることがあります。
  </p>

  <pre><code>ipconfig getifaddr en1</code></pre>

  <p>例：</p>

  <pre><code>192.168.1.10</code></pre>

  <p>
    他の端末では、MinecraftのサーバーアドレスにこのIPアドレスを入力します。
  </p>

  <h3>10. 外部の友達に接続してもらう場合</h3>

  <p>
    家の外にいる友達に接続してもらうには、
    通常はルーター側でポート開放が必要です。
  </p>

  <p>
    Minecraft Java版サーバーの標準ポートは次の番号です。
  </p>

  <pre><code>25565</code></pre>

  <p>
    ルーターの管理画面で、サーバーを立てているMacに対して、
    TCPの25565番ポートを開放します。
  </p>

  <p>
    ただし、ポート開放はネットワーク設定やセキュリティに関わる作業です。
    よく分からない場合は、無理に設定しないほうが安全です。
    身内だけで遊ぶ場合は、VPN系のサービスやレンタルサーバーを使う方法もあります。
  </p>

  <h3>11. サーバーを終了する方法</h3>

  <p>
    サーバーを終了するときは、ターミナルをそのまま閉じるのではなく、
    ターミナルに次のコマンドを入力します。
  </p>

  <pre><code>stop</code></pre>

  <p>
    これでワールドデータが保存され、安全にサーバーを停止できます。
  </p>
</section>
      `;
    }